kind: pipeline
type: docker
name: docker-build

steps:
  - name: build-and-push
    image: plugins/docker
    settings:
      repo: mb2214/dronexdocker
      tags: latest
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
